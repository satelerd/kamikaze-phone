# Set this on the Convex deployment, not in the iOS app environment.
# Activate Clerk's Convex integration first; use the resulting Frontend API
# URL exactly (including scheme and host). It is public issuer metadata, not a
# Clerk secret key.
CLERK_FRONTEND_API_URL=https://your-clerk-frontend-api.example
