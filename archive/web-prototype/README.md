# Original web prototype

This is the first browser/Next.js exploration of Kamikaze: Phone Flip. It predates the Expo motion laboratory and is preserved as historical, runnable code.

```sh
npm install
npm run dev
```

Browser sensor behavior is not a reference for the current detector. Use `apps/expo` for Android or Expo Go work and `apps/ios` for the native iOS beta.
