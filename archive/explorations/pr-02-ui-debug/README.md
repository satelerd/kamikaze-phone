# PR #2 — UI and debug variation

Historical snapshot of [PR #2](https://github.com/satelerd/kamikaze-phone/pull/2), **Improve game UI and add debug**.

- Original commit: `fe75f3c`
- Historical tag: `exploration-ui-debug-v0.1.0`
- Status: superseded by the Expo motion laboratory, preserved for its alternate game UI, debug flow and sensor-permission hook.

Run the snapshot:

```sh
cd source
npm install
npm run dev
```

The browser sensor assumptions and scoring are exploratory and should not be treated as current detector behavior.
The original dependency lock also contains known audit findings and is preserved for reproducibility, not production deployment.
