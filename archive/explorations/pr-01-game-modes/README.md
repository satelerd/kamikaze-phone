# PR #1 — Basic game modes

Historical snapshot of [PR #1](https://github.com/satelerd/kamikaze-phone/pull/1), **Add basic Kamikaze Phone game modes**.

- Original commit: `64828dd`
- Historical tag: `exploration-game-modes-v0.1.0`
- Status: superseded by the Expo motion laboratory, preserved for its early Kamikaze Classic and Trick Throw interaction ideas.

Run the snapshot:

```sh
cd source
npm install
npm run dev
```

The browser sensor assumptions and scoring are exploratory and should not be treated as current detector behavior.
The original dependency lock also contains known audit findings and is preserved for reproducibility, not production deployment.
